"""FastAPI application entry point for salary prediction.

Exposes a POST endpoint that accepts job details, applies preprocessing,
loads the trained Decision Tree model, and returns the predicted salary
in USD.
"""
